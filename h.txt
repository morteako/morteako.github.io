module Main exposing (..)

import Html exposing (..)
import Htm.Attributes exposing (..)




type State = Open | Closed | Matched

type card = {
	id: String,
	state: State
}

viewCard card =
  img [(src card.string)] []

viewCards cards =
  List.map viewCard cards

main = text "1"
